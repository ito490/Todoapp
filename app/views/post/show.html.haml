.main.posts-show
  .container
    .posts-show-item
      %p
        = @post.content
      .post-time
        = @post.created_at
        .post-menus
          = link_to("編集", "/post/#{@post.id}/edit")
          = link_to("削除", "/post/#{@post.id}/destroy",{method:"post"})